import { RootState } from "../index";
export const counter = (state: RootState) => state.counter.value;

// export const count = () => state.count.value;

// export type RootState = ReturnType<typeof count>;
